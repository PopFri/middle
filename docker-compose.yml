services:
  popfri:
    build: ../FrontEnd
    ports:
      - "5173:5173"
    command: ["npm", "run", "preview"]
  popfri-backend:
    build: ../BackEnd
    ports:
      - "8081:8081"
    volumes:
      - ../BackEnd/.env:/app/.env